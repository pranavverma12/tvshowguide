<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @show.name %>
</p>

<p>
  <strong>Channel:</strong>
  <%= @show.channel_id %>
</p>

<p>
  <strong>Timing:</strong>
  <%= @show.timing.strftime("%I:%M %p") %>
</p>

<%#= link_to 'Edit', edit_show_path(@show) %>
<%= link_to 'Back', shows_path %>
